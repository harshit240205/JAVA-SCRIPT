// Immidiatetly Invoked Function Expressions (IIFE)

// Sometimes we have issue witht he Global scope pollution so to remove that pollution we will use IIFE

(function chai(){
    console.log(`DB CONNECTED`);
    
})();